import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1
    },
    titulo: {
        marginTop: 100,
        alignItems: "center",
        color: "#B5933B",
        fontSize: 50
    }
})
